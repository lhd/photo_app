<%= form_for(@image_order) do |f| %>
  <% if @image_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_order.errors.count, "error") %> prohibited this image_order from being saved:</h2>

      <ul>
      <% @image_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
